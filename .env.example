# Environment Variables Template
# Copy this to .env and fill in your values

# ============================================
# Django Settings
# ============================================
SECRET_KEY=your-secret-key-here-generate-a-long-random-string
DEBUG=True  # Set to False in production

# ============================================
# Database Configuration
# ============================================
# For local development, leave empty to use SQLite
# For production (Render), use PostgreSQL connection string
# Format: postgresql://user:password@host:port/dbname
DATABASE_URL=

# ============================================
# Admin User Configuration (Production)
# ============================================
# These are used by the ensure_admin command during deployment
# Render will auto-generate ADMIN_PASSWORD if you use generateValue in render.yaml
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-this-to-secure-password

# ============================================
# Allowed Hosts (Production)
# ============================================
# Comma-separated list of allowed hosts
# For Render: your-app.onrender.com,localhost
# For local development, can leave empty (defaults to all)
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================
# Optional: Security Settings for Production
# ============================================
# These are automatically configured in settings.py when DEBUG=False
# SECURE_SSL_REDIRECT handled by Render proxy
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True

# ============================================
# Email Configuration (For Password Reset)
# ============================================
# For DEVELOPMENT: Leave EMAIL_BACKEND empty (uses console backend - prints to terminal)
# For PRODUCTION: Configure SMTP settings below

# Email Backend (optional, defaults to console for dev)
# For production with Gmail: django.core.mail.backends.smtp.EmailBackend
EMAIL_BACKEND=

# SMTP Settings (Required for production password reset emails)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True

# Gmail Account (Use App-Specific Password, NOT your regular password)
# Steps to get App Password:
# 1. Go to https://myaccount.google.com/security
# 2. Enable 2-Step Verification if not already enabled
# 3. Go to https://myaccount.google.com/apppasswords
# 4. Select 'Mail' and 'Other (Custom name)' - name it "Journal App"
# 5. Copy the 16-character password and paste below
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-16-char-app-password

# From Email Address (defaults to EMAIL_HOST_USER if not set)
DEFAULT_FROM_EMAIL=Journal Pro <your-email@gmail.com>

# ============================================
# Google OAuth Configuration
# ============================================
# Get these from Google Cloud Console (APIs & Services > Credentials)
# 1. Create OAuth 2.0 Client ID (Web application)
# 2. Add authorized redirect URIs:
#    - Local: http://localhost:8000/accounts/google/login/callback/
#    - Production: https://your-app.onrender.com/accounts/google/login/callback/
# 3. Copy Client ID and Client Secret below

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
